using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// 接口 interface
///     1.接口只声明接口函数，不包含实现
///     2.接口函数访问修饰符 必须是public，默认是public
///     3.接口成员函数的定义时派生类的责任，接口提供了派生类应遵循的标准结构
///     4.接口不可被实例化，即不可new
///     5.接口可继承其他接口
/// </summary>
public class TInterfact : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
